# Pokedex KMP (= Kotlin Multi Platform)

This is the common library that is used by all Pokedex applications.

To build, run `./gradlew build`.

To deploy to maven local, run `./gradlew publishToMavenLocal`
